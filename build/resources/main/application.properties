#####################################################################################################
# Postgres properties
#
# In Postgres, you can specify the schema in the URL (see docs).
#####################################################################################################
spring.datasource.driver-class-name= org.postgresql.Driver
spring.datasource.url = jdbc:postgresql://localhost:5432/northstar?currentSchema=sgoc
spring.datasource.username = postgres
spring.datasource.password = postgres
